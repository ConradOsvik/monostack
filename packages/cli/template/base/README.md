# {{projectName}}

A fullstack monorepo built with [Monostack](https://github.com/monostack/monostack).

## Getting Started

```bash
bun dev
```

## Project Structure

- `apps/web` — TanStack Start web application
- `apps/native` — Expo native application
- `packages/api` — tRPC API layer
- `packages/env` — Environment variable validation (t3-env)
- `packages/config` — Shared TypeScript configs
